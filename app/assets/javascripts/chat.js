$(document).ready(function () {
  var $submitButton = $('.submit');
  var $message = $('.message');

  //$submitButton.on('click', function () {
    //$.ajax({
      //method: "POST",
      //url: "/messages",
      //success: function (data) {
        //alert(data);
      //}
    //});
  //});
});
